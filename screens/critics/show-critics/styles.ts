import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    gap: 10,
    paddingBottom: 10,
  },
  showCritics: {
    flex: 1,
  },
  wrapper: {
    marginBottom: 90,
  },
});
