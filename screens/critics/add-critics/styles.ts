import { StyleSheet } from "react-native";

export default StyleSheet.create({
  heading: {
    fontSize: 20,
    marginBottom: 10,
  },
  addCritics: {
    flex: 1,
  },
  wrapper: {
    gap: 20,
  },
});
