import { StyleSheet } from "react-native";

export default StyleSheet.create({
  reports: {
    gap: 10,
  },
  wrapper: {
    flexDirection: "row",
    justifyContent: "center",
    gap: 20,
  },
});
