import { StyleSheet } from "react-native";

export default StyleSheet.create({
  carousel: {
    width: "100%",
  },
  item: {
    flex: 1,
    alignItems: "center",
    marginTop: -20,
    gap: 20,
  },
});
