import { StyleSheet } from "react-native";

export default StyleSheet.create({
  addToCart: {
    alignItems: "center",
    width: "60%",
    gap: 10,
  },
  btn: {
    borderRadius: 100,
    paddingVertical: 15,
    padding: 10,
  },
});
