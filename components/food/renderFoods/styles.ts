import { StyleSheet } from "react-native";

export default StyleSheet.create({
  icon: {
    borderRadius: 16,
    width: 80,
    height: 80,
  },
  renderFoods: {
    backgroundColor: "#eee",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "flex-end",
    borderRadius: 10,
    paddingLeft: 20,
    gap: 20,
  },
});
