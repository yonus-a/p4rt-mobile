import { StyleSheet } from "react-native";

export default StyleSheet.create({
  quickPanel: {
    position: "absolute",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    gap: 40,
    height: 65,
    left: 0,
    bottom: 10,
    right: 0,
    paddingLeft: 30,
    paddingTop: 10,
  },
  image: {
    width: 30,
    height: 30,
  },
});
