import { StyleSheet } from "react-native";

export default StyleSheet.create({
  quickPanel: {
    position: "absolute",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    padding: 30,
    width: 400,
    gap: 55,
    bottom: 0,
  },
  image: {
    width: 30,
    height: 30,
  },
});
