import { StyleSheet } from "react-native";

export default StyleSheet.create({
  chart: {
    width: "100%",
    alignItems: "center",
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.27,
    shadowRadius: 4.65,
    borderRadius: 16,
    elevation: 5,
  },
});
