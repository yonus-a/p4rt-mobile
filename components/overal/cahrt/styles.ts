import { StyleSheet } from "react-native";

export default StyleSheet.create({
  chart: {
    width: "100%",
    alignItems: "center",
    shadowColor: "#111",
    borderRadius: 16,
    elevation: 8,
  },
});
