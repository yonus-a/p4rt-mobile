import { StyleSheet } from "react-native";

export default StyleSheet.create({
  btn: {
    backgroundColor: "#eee",
    borderRadius: 10,
    padding: 20,
  },
  btnText: {
    textAlign: "center",
    fontWeight: "bold",
  },
});
