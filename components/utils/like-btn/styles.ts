import { StyleSheet } from "react-native";

export default StyleSheet.create({
  likeBtn: {
    flexDirection: "row",
    alignItems: "center",
    gap: 3,
  },
  image: {
    width: 30,
    height: 30,
  },
});
