import { StyleSheet } from "react-native";

export default StyleSheet.create({
  datepicker: {},
  dateInput: {
    padding: 10,
    backgroundColor: "#eee",
    borderRadius: 8,
    color: "#555",
  },
});
