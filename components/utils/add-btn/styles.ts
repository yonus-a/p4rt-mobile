import { StyleSheet } from "react-native";

export default StyleSheet.create({
  addBtn: {
    flexDirection: "row",
    backgroundColor: "#ddd",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 8,
    padding: 12.5,
    gap: 8,
  },
  text: {
    fontSize: 13,
  },
  icon: {
    width: 24,
    height: 24,
  },
});
