import { StyleSheet } from "react-native";

export default StyleSheet.create({
  addBtn: {
    flexDirection: "row",
    backgroundColor: "#ddd",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 8,
    padding: 14,
    gap: 8,
  },
  text: {
    fontSize: 15,
  },
  icon: {
    width: 24,
    height: 24,
  },
});
