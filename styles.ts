import { StyleSheet } from "react-native";

export const CLR_PRIMARY = "#203270";
export const CLR_WHITE = "#ffffff";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    alignItems: "center",
  },
});
